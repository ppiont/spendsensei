# Public environment variables (accessible to client-side code)
# These MUST be declared here so SvelteKit generates proper types
# Railway will override these values with actual environment variables

# Backend API URL
PUBLIC_API_BASE_URL=

# Railway auto-provided service URLs (optional, for reference)
PUBLIC_RAILWAY_SERVICE_SPENDSENSEI_URL=
