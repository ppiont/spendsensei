# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
    "npm install -g bun",
    "bun install"
]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "node build/index.js"
